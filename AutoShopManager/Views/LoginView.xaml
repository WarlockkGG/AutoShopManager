<Window x:Class="AutoShopManager.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Авторизация AutoShop Manager" 
        Height="450" Width="400"
        WindowStartupLocation="CenterScreen" 
        ResizeMode="NoResize">

    <Border Padding="25" Background="#F0F0F0">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="300">

            <TextBlock Text="AutoShop Manager" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="#2196F3"
                       HorizontalAlignment="Center" 
                       Margin="0,0,0,30"/>

            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" 
                     Height="40" 
                     Margin="0,0,0,15" 
                     FontSize="14"
                     VerticalContentAlignment="Center"
                     ToolTip="Введите имя пользователя"
                     Padding="10,0"/>

            <PasswordBox x:Name="PasswordInput" 
                         Height="40" 
                         Margin="0,0,0,25"
                         FontSize="14"
                         ToolTip="Введите пароль"/>

            <TextBlock Text="{Binding ErrorMessage}" 
                       Foreground="Red" 
                       HorizontalAlignment="Center" 
                       TextWrapping="Wrap"
                       Margin="0,0,0,20"/>

            <Button Content="Увійти" 
                    Height="40" 
                    Background="#2196F3"
                    Foreground="White"
                    FontSize="16"
                    Command="{Binding LoginCommand}"
                    CommandParameter="{Binding ElementName=PasswordInput, Path=Password}">
            </Button>

        </StackPanel>
    </Border>
</Window>